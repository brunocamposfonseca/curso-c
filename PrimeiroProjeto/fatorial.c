#include <stdlib.h>
#include <stdio.h>

/*
int main(){
    int num = 0;
    int res = 1;

    scanf("%i", &num);

    for (int x = num ; x >= 1; --x){
        res *= x;
    }

    printf("O fatorial de %i é: %i\n", num, res);

    system("pause");
    return 0;
}
*/
